# Настройка SMTP для отправки email

Эта документация описывает, как настроить отправку email-уведомлений в Stogram для верификации email и восстановления пароля.

## Обзор

Stogram использует `nodemailer` для отправки email через SMTP. При регистрации пользователя автоматически отправляется письмо с ссылкой для верификации email адреса.

## Переменные окружения

Для работы отправки email необходимо настроить следующие переменные окружения:

| Переменная | Описание | Обязательная | Пример |
|------------|----------|--------------|--------|
| `SMTP_HOST` | SMTP сервер | Да | `smtp.gmail.com` |
| `SMTP_PORT` | Порт SMTP | Да | `587` (TLS) или `465` (SSL) |
| `SMTP_USER` | Email для авторизации | Да | `your-email@gmail.com` |
| `SMTP_PASS` | Пароль или App Password | Да | `your-app-password` |
| `SMTP_SECURE` | Использовать SSL | Нет | `false` (для TLS) или `true` (для SSL) |
| `SMTP_FROM` | Email отправителя | Нет | `noreply@yourdomain.com` (по умолчанию = `SMTP_USER`) |
| `CLIENT_URL` | URL фронтенда для ссылок | Да | `https://stogram-client.vercel.app` |

## Настройка на Render

### Шаг 1: Добавление переменных окружения

1. Откройте [Render Dashboard](https://dashboard.render.com)
2. Перейдите в ваш сервис `stogram-server`
3. Откройте раздел **Environment**
4. Добавьте все необходимые SMTP переменные (см. примеры ниже)
5. Нажмите **Save Changes**
6. Выполните **Manual Deploy** → **Deploy latest commit**

### Шаг 2: Проверка работы

После деплоя проверьте логи сервера. При регистрации нового пользователя вы должны увидеть:
```
Verification email sent to user@example.com
```

Если письмо не отправляется, проверьте логи на наличие ошибок:
```
Failed to send verification email: [error details]
```

---

## Примеры настройки для популярных провайдеров

### 📧 Gmail

Gmail требует использования **App Password** вместо обычного пароля.

#### Шаг 1: Создание App Password

1. Войдите в [Google Account](https://myaccount.google.com/)
2. Перейдите в **Security** → **2-Step Verification** (должна быть включена)
3. Прокрутите вниз до **App passwords**
4. Выберите **Mail** и **Other (Custom name)**
5. Введите название: `Stogram`
6. Нажмите **Generate**
7. **Скопируйте** сгенерированный пароль (16 символов без пробелов)

#### Шаг 2: Настройка переменных в Render

```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=xxxx xxxx xxxx xxxx  # App Password (16 символов)
SMTP_FROM=your-email@gmail.com
CLIENT_URL=https://stogram-client.vercel.app
```

**Важно:**
- Используйте **App Password**, а не обычный пароль
- `SMTP_SECURE=false` для порта 587 (TLS)
- Для порта 465 используйте `SMTP_SECURE=true`

#### Альтернатива: Gmail через OAuth2

Для продакшена рекомендуется использовать OAuth2 вместо App Password. Это требует дополнительной настройки в `emailService.ts`.

---

### 📧 Яндекс.Почта

Яндекс также требует использования **Пароля приложения**.

#### Шаг 1: Создание пароля приложения

1. Войдите в [Яндекс ID](https://id.yandex.ru/)
2. Перейдите в **Безопасность** → **Пароли приложений**
3. Нажмите **Создать новый пароль**
4. Введите название: `Stogram`
5. Нажмите **Создать**
6. **Скопируйте** сгенерированный пароль

#### Шаг 2: Настройка переменных в Render

```env
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your-email@yandex.ru
SMTP_PASS=xxxx xxxx xxxx xxxx  # Пароль приложения
SMTP_FROM=your-email@yandex.ru
CLIENT_URL=https://stogram-client.vercel.app
```

**Важно:**
- Используйте **Пароль приложения**, а не основной пароль
- `SMTP_SECURE=true` для порта 465 (SSL)
- Альтернативно можно использовать порт 587 с `SMTP_SECURE=false`

#### Альтернативные настройки (порт 587)

```env
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@yandex.ru
SMTP_PASS=xxxx xxxx xxxx xxxx
SMTP_FROM=your-email@yandex.ru
CLIENT_URL=https://stogram-client.vercel.app
```

---

### 📧 MAIL.RU

MAIL.RU также использует **Пароль приложения**.

#### Шаг 1: Создание пароля приложения

1. Войдите в [Mail.ru ID](https://id.mail.ru/)
2. Перейдите в **Безопасность** → **Пароли приложений**
3. Нажмите **Создать пароль приложения**
4. Введите название: `Stogram`
5. Нажмите **Создать**
6. **Скопируйте** сгенерированный пароль

#### Шаг 2: Настройка переменных в Render

```env
SMTP_HOST=smtp.mail.ru
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your-email@mail.ru
SMTP_PASS=xxxx xxxx xxxx xxxx  # Пароль приложения
SMTP_FROM=your-email@mail.ru
CLIENT_URL=https://stogram-client.vercel.app
```

**Важно:**
- Используйте **Пароль приложения**, а не основной пароль
- `SMTP_SECURE=true` для порта 465 (SSL)
- Альтернативно можно использовать порт 587 с `SMTP_SECURE=false`

#### Альтернативные настройки (порт 587)

```env
SMTP_HOST=smtp.mail.ru
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@mail.ru
SMTP_PASS=xxxx xxxx xxxx xxxx
SMTP_FROM=your-email@mail.ru
CLIENT_URL=https://stogram-client.vercel.app
```

---

## Другие популярные провайдеры

### Outlook / Hotmail

```env
SMTP_HOST=smtp-mail.outlook.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@outlook.com
SMTP_PASS=your-password
SMTP_FROM=your-email@outlook.com
CLIENT_URL=https://stogram-client.vercel.app
```

### SendGrid

```env
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=apikey
SMTP_PASS=SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx  # SendGrid API Key
SMTP_FROM=noreply@yourdomain.com
CLIENT_URL=https://stogram-client.vercel.app
```

### Mailgun

```env
SMTP_HOST=smtp.mailgun.org
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=postmaster@mg.yourdomain.com
SMTP_PASS=your-mailgun-password
SMTP_FROM=noreply@yourdomain.com
CLIENT_URL=https://stogram-client.vercel.app
```

### Amazon SES

```env
SMTP_HOST=email-smtp.us-east-1.amazonaws.com  # Замените на ваш регион
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-ses-smtp-username
SMTP_PASS=your-ses-smtp-password
SMTP_FROM=noreply@yourdomain.com
CLIENT_URL=https://stogram-client.vercel.app
```

---

## Локальная разработка

Для локальной разработки создайте файл `server/.env`:

```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=your-email@gmail.com
CLIENT_URL=http://localhost:5173
```

**Важно:** Не коммитьте файл `.env` в Git! Он уже добавлен в `.gitignore`.

---

## Тестирование отправки email

### Через регистрацию

1. Откройте страницу регистрации: `https://stogram-client.vercel.app/register`
2. Заполните форму и зарегистрируйтесь
3. Проверьте почтовый ящик (включая папку "Спам")
4. Откройте письмо и перейдите по ссылке верификации

### Проверка логов

В логах Render вы должны увидеть:
```
Verification email sent to user@example.com
```

Если есть ошибки:
```
Failed to send verification email: [error message]
```

### Типичные ошибки

1. **"Invalid login"** или **"Authentication failed"**
   - Проверьте правильность `SMTP_USER` и `SMTP_PASS`
   - Убедитесь, что используете App Password, а не основной пароль

2. **"Connection timeout"**
   - Проверьте `SMTP_HOST` и `SMTP_PORT`
   - Убедитесь, что порт не заблокирован файрволом

3. **"Self signed certificate"**
   - Установите `SMTP_SECURE=true` для порта 465
   - Или `SMTP_SECURE=false` для порта 587

4. **"Email not sent"**
   - Проверьте, что `SMTP_USER` установлен (проверка в коде: `if (process.env.SMTP_USER)`)
   - Проверьте логи на наличие ошибок

---

## Безопасность

### Рекомендации

1. **Используйте App Passwords** вместо основных паролей
2. **Не коммитьте** SMTP credentials в Git
3. **Используйте переменные окружения** для всех секретов
4. **Ограничьте доступ** к SMTP аккаунту (отдельный email для приложения)
5. **Мониторьте логи** на подозрительную активность

### Ограничения провайдеров

- **Gmail**: до 500 писем в день (бесплатный аккаунт)
- **Яндекс**: до 500 писем в день (бесплатный аккаунт)
- **MAIL.RU**: до 1000 писем в день (бесплатный аккаунт)

Для продакшена с большим объемом писем рекомендуется использовать специализированные сервисы:
- SendGrid (100 писем/день бесплатно)
- Mailgun (5000 писем/месяц бесплатно)
- Amazon SES (62,000 писем/месяц бесплатно)

---

## Дополнительная информация

### Структура письма верификации

Письмо содержит:
- Приветствие с именем пользователя
- Кнопку "Verify Email"
- Текстовую ссылку для копирования
- Информацию о сроке действия (24 часа)

### Ссылка верификации

Формат: `${CLIENT_URL}/verify-email?token=${verificationToken}`

Пример: `https://stogram-client.vercel.app/verify-email?token=abc123...`

### Истечение токена

Токен верификации действителен **24 часа**. После истечения пользователь должен запросить новое письмо (функционал пока не реализован).

---

## Поддержка

Если у вас возникли проблемы с настройкой SMTP:

1. Проверьте логи сервера в Render Dashboard
2. Убедитесь, что все переменные окружения установлены правильно
3. Проверьте, что используете App Password, а не основной пароль
4. Проверьте папку "Спам" в почтовом ящике
5. Убедитесь, что `CLIENT_URL` указывает на правильный домен фронтенда

---

## Обновление переменных в Render

После добавления/изменения переменных окружения:

1. Сохраните изменения в Render Dashboard
2. Выполните **Manual Deploy** → **Deploy latest commit**
3. Дождитесь завершения деплоя
4. Проверьте логи на наличие ошибок
5. Протестируйте регистрацию нового пользователя

---

**Последнее обновление:** 2026-02-06
