# 🧪 Локальное тестирование проекта Stogram

Это подробное руководство по локальному тестированию проекта Stogram - как автоматическому (unit/integration тесты), так и ручному.

---

## 📋 Содержание

- [Подготовка окружения](#подготовка-окружения)
- [Запуск проекта локально](#запуск-проекта-локально)
- [Автоматическое тестирование](#автоматическое-тестирование)
- [Ручное тестирование](#ручное-тестирование)
- [Тестирование API](#тестирование-api)
- [Тестирование базы данных](#тестирование-базы-данных)
- [Тестирование WebSocket](#тестирование-websocket)
- [Решение проблем](#решение-проблем)

---

## 🚀 Подготовка окружения

### Системные требования

Убедитесь, что у вас установлено:

- **Node.js** 18 или выше
- **npm** (поставляется с Node.js)
- **PostgreSQL** 15 или выше
- **Redis** (опционально, но рекомендуется)
- **Docker и Docker Compose** (опционально)
- **Git**

### Проверка установленных версий

```bash
node --version    # должна быть >= 18
npm --version
git --version
docker --version  # опционально
```

---

## 🏃 Запуск проекта локально

### Вариант 1: Быстрый старт с Docker (рекомендуется)

Это самый простой способ запустить проект для тестирования.

```bash
# Клонируйте репозиторий (если еще не клонировали)
git clone <repository-url>
cd stogram

# Запустите автоматический скрипт установки
chmod +x quick-start.sh
./quick-start.sh
```

Скрипт автоматически:
- Создаст файлы `.env` из шаблонов
- Сгенерирует безопасный JWT секрет
- Запустит Docker контейнеры (PostgreSQL, Redis, Backend, Frontend)
- Выполнит миграции базы данных

После успешного запуска:
- 🌐 **Frontend**: http://localhost:5173
- 🔌 **Backend API**: http://localhost:3001
- 🏥 **Health Check**: http://localhost:3001/health

### Вариант 2: Ручная установка для разработки

Используйте этот способ, если хотите запускать компоненты отдельно:

#### Шаг 1: Установка зависимостей

```bash
# Установка всех зависимостей (клиент + сервер)
npm run install:all
```

#### Шаг 2: Настройка переменных окружения

**Сервер** (`server/.env`):
```bash
cd server
cp .env.example .env
nano .env  # или используйте VS Code
```

Минимально необходимые настройки:
```env
# База данных
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/stogram"

# JWT секрет (сгенерируйте уникальный!)
JWT_SECRET="your-secret-key-min-32-chars-long-change-this"

# Порты
PORT=3001

# CORS
CLIENT_URL="http://localhost:5173"

# Redis (опционально)
REDIS_URL="redis://localhost:6379"
REDIS_ENABLED=true

# Загрузка файлов
UPLOAD_DIR="./uploads"
MAX_FILE_SIZE=10485760  # 10MB
```

**Клиент** (`client/.env`):
```bash
cd ../client
cp .env.example .env
nano .env
```

```env
VITE_API_URL=http://localhost:3001
VITE_WS_URL=ws://localhost:3001
```

#### Шаг 3: Запуск базы данных

**С Docker (рекомендуется):**
```bash
cd ..
docker-compose up -d postgres redis
```

**Без Docker:**
```bash
# Запустите PostgreSQL
sudo systemctl start postgresql

# Создайте базу данных
createdb stogram

# Запустите Redis
sudo systemctl start redis
```

#### Шаг 4: Миграции базы данных

```bash
cd server
npx prisma migrate dev
npx prisma generate
cd ..
```

#### Шаг 5: Запуск серверов разработки

**Запустить всё одновременно:**
```bash
npm run dev
```

**Или запустить отдельно:**

В одном терминале (сервер):
```bash
npm run dev:server
```

В другом терминале (клиент):
```bash
npm run dev:client
```

---

## 🧪 Автоматическое тестирование

### Тестирование клиента (Frontend)

Клиент использует **Vitest** для тестирования React компонентов.

```bash
cd client

# Запустить все тесты
npm run test

# Запустить тесты в watch режиме (перезапуск при изменениях)
npm run test -- --watch

# Запустить тесты с UI интерфейсом
npm run test:ui

# Запустить с coverage отчетом
npm run test:coverage
```

**Примеры тестирования:**

```typescript
// Пример: client/src/components/__tests__/Button.test.tsx
import { render, screen, fireEvent } from '@testing-library/react';
import { Button } from '../Button';

describe('Button Component', () => {
  it('renders button with text', () => {
    render(<Button>Click me</Button>);
    expect(screen.getByText('Click me')).toBeInTheDocument();
  });

  it('calls onClick handler', () => {
    const handleClick = vi.fn();
    render(<Button onClick={handleClick}>Click</Button>);
    fireEvent.click(screen.getByText('Click'));
    expect(handleClick).toHaveBeenCalledOnce();
  });
});
```

### Тестирование сервера (Backend)

Сервер использует **Jest** и **Supertest** для тестирования API.

```bash
cd server

# Запустить все тесты
npm run test

# Запустить тесты в watch режиме
npm run test:watch

# Запустить с coverage отчетом
npm run test:coverage
```

**Примеры тестирования API:**

```typescript
// Пример: server/src/__tests__/auth.test.ts
import request from 'supertest';
import app from '../app';

describe('Auth API', () => {
  describe('POST /api/auth/register', () => {
    it('should register a new user', async () => {
      const res = await request(app)
        .post('/api/auth/register')
        .send({
          email: 'test@example.com',
          username: 'testuser',
          password: 'password123'
        });
      
      expect(res.status).toBe(201);
      expect(res.body).toHaveProperty('token');
      expect(res.body.user).toHaveProperty('id');
    });

    it('should reject invalid email', async () => {
      const res = await request(app)
        .post('/api/auth/register')
        .send({
          email: 'invalid-email',
          username: 'testuser',
          password: 'password123'
        });
      
      expect(res.status).toBe(400);
    });
  });
});
```

### Просмотр coverage отчетов

После запуска `npm run test:coverage`:

```bash
# Клиент
open client/coverage/index.html

# Сервер
open server/coverage/index.html
```

---

## 🖱️ Ручное тестирование

### 1. Создание тестового пользователя

1. Откройте http://localhost:5173
2. Нажмите **"Регистрация"**
3. Заполните форму:
   - **Email**: test@example.com
   - **Имя пользователя**: testuser
   - **Пароль**: test123456
4. Нажмите **"Зарегистрироваться"**

Вы автоматически войдете в систему!

### 2. Тестирование аутентификации

#### Регистрация
- ✅ Успешная регистрация с валидными данными
- ✅ Ошибка при существующем email
- ✅ Ошибка при существующем username
- ✅ Ошибка при слабом пароле
- ✅ Валидация email формата

#### Вход в систему
- ✅ Вход по email и паролю
- ✅ Вход по username и паролю
- ✅ Ошибка при неверных данных
- ✅ Сохранение сессии (JWT token)

#### Выход
- ✅ Выход из системы
- ✅ Очистка токена
- ✅ Перенаправление на страницу входа

### 3. Тестирование профиля

1. Откройте **Настройки** (иконка шестеренки)
2. Перейдите в **"Мой профиль"**

Протестируйте:
- ✅ Загрузка аватара
- ✅ Изменение отображаемого имени
- ✅ Изменение био
- ✅ Изменение статуса (Онлайн/Отошёл/Не беспокоить)
- ✅ Настройки приватности

### 4. Тестирование чатов

#### Создание чата
```
1. Нажмите кнопку "+" в списке чатов
2. Выберите тип чата:
   - Приватный чат (1-на-1)
   - Групповой чат (несколько участников)
   - Канал (один пишет, все читают)
3. Добавьте участников
4. Нажмите "Создать"
```

#### Отправка сообщений
- ✅ Текстовое сообщение
- ✅ Сообщение с эмодзи
- ✅ Упоминание пользователя (@username)
- ✅ Хэштеги (#тег)
- ✅ Ответ на сообщение (Reply)
- ✅ Редактирование сообщения
- ✅ Удаление сообщения

#### Отправка медиа
```
Нажмите иконку скрепки и выберите:
- 📷 Изображения (JPG, PNG, GIF)
- 🎥 Видео (MP4, WEBM)
- 🎵 Аудио файлы
- 📄 Документы
- 🎤 Голосовое сообщение (запись)
```

Проверьте:
- ✅ Предпросмотр перед отправкой
- ✅ Прогресс загрузки
- ✅ Автоматическое сжатие изображений
- ✅ Генерация превью для видео
- ✅ Воспроизведение медиа в чате

### 5. Тестирование групповых функций

В групповом чате:
- ✅ Добавление участников
- ✅ Удаление участников
- ✅ Изменение роли (Admin/Member)
- ✅ Изменение имени группы
- ✅ Изменение аватара группы
- ✅ Выход из группы

### 6. Тестирование стикеров

1. Откройте **Настройки → Стикеры**
2. Создайте пак стикеров:
   - Название: "Мои стикеры"
   - Загрузите 3-5 изображений стикеров
3. В чате нажмите иконку стикера
4. Выберите и отправьте стикер

Проверьте:
- ✅ Создание пака
- ✅ Загрузка стикеров
- ✅ Отправка стикера в чате
- ✅ Публичные/приватные паки

### 7. Тестирование звонков

⚠️ **Требуется два пользователя в разных браузерах**

#### Аудио звонок
1. Откройте приватный чат
2. Нажмите иконку телефона
3. В другом браузере примите звонок
4. Проверьте качество звука

#### Видео звонок
1. Откройте приватный чат
2. Нажмите иконку видеокамеры
3. В другом браузере примите звонок
4. Проверьте качество видео и звука

Тестируйте:
- ✅ Отключение микрофона
- ✅ Отключение видео
- ✅ Завершение звонка
- ✅ Отклонение звонка
- ✅ История звонков

### 8. Тестирование ботов

#### Создание бота
```bash
1. Настройки → Боты → Создать бота
2. Заполните:
   - Username: mybot
   - Отображаемое имя: My Test Bot
   - Описание: Test bot
3. Скопируйте токен бота
```

#### Тестирование Bot API
```bash
# Используйте скопированный токен
curl -X POST http://localhost:3001/api/bot/YOUR_BOT_TOKEN/sendMessage \
  -H "Content-Type: application/json" \
  -d '{
    "chatId": "CHAT_ID",
    "text": "Hello from bot!"
  }'
```

### 9. Тестирование поиска

1. Отправьте несколько сообщений с уникальными словами
2. Используйте поиск (иконка поиска в шапке)
3. Проверьте:
   - ✅ Поиск по тексту сообщений
   - ✅ Поиск по имени пользователя
   - ✅ Поиск по названию чата
   - ✅ Фильтрация результатов

### 10. Тестирование уведомлений

1. Откройте **Настройки → Уведомления**
2. Разрешите браузерные уведомления
3. В другом браузере отправьте сообщение
4. Проверьте:
   - ✅ Push-уведомление в браузере
   - ✅ Звук уведомления
   - ✅ Бейдж с количеством непрочитанных
   - ✅ Отключение уведомлений для чата

### 11. Тестирование PWA

#### Установка PWA
**Chrome/Edge:**
1. Откройте http://localhost:5173
2. Нажмите "Установить" в адресной строке
3. Подтвердите установку

**Safari (iOS/macOS):**
1. Откройте http://localhost:5173
2. Нажмите кнопку "Поделиться"
3. Выберите "Добавить на главный экран"

#### Тестирование офлайн режима
1. Откройте DevTools (F12)
2. Перейдите в **Application → Service Workers**
3. Включите **Offline**
4. Обновите страницу
5. Проверьте:
   - ✅ Приложение загружается офлайн
   - ✅ Кэшированные чаты доступны
   - ✅ Сообщения ставятся в очередь
   - ✅ При восстановлении связи сообщения отправляются

---

## 🔌 Тестирование API

### Использование curl

#### Регистрация
```bash
curl -X POST http://localhost:3001/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "username": "testuser",
    "password": "password123"
  }'
```

#### Вход
```bash
curl -X POST http://localhost:3001/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "password": "password123"
  }'
```

Сохраните полученный токен:
```bash
TOKEN="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
```

#### Получение профиля
```bash
curl http://localhost:3001/api/users/me \
  -H "Authorization: Bearer $TOKEN"
```

#### Получение чатов
```bash
curl http://localhost:3001/api/chats \
  -H "Authorization: Bearer $TOKEN"
```

### Использование Postman

1. Импортируйте коллекцию (если есть)
2. Установите переменную окружения:
   - `BASE_URL`: http://localhost:3001
   - `TOKEN`: (получите после логина)

### Использование Thunder Client (VS Code)

1. Установите расширение Thunder Client
2. Создайте новый запрос
3. Добавьте Authorization header с токеном

---

## 🗄️ Тестирование базы данных

### Prisma Studio (GUI)

```bash
cd server
npx prisma studio
```

Откроется http://localhost:5555 с GUI для просмотра и редактирования данных.

### Проверка данных через SQL

```bash
# Подключение к PostgreSQL
docker-compose exec postgres psql -U postgres -d stogram

# Или локально
psql -U postgres -d stogram
```

Полезные SQL запросы:

```sql
-- Все пользователи
SELECT id, username, email, "displayName", "createdAt" FROM "User";

-- Все чаты
SELECT id, name, type, "createdAt" FROM "Chat";

-- Последние сообщения
SELECT m.id, m.content, u.username, c.name, m."createdAt"
FROM "Message" m
JOIN "User" u ON m."senderId" = u.id
JOIN "Chat" c ON m."chatId" = c.id
ORDER BY m."createdAt" DESC
LIMIT 10;

-- Количество сообщений по пользователям
SELECT u.username, COUNT(m.id) as message_count
FROM "User" u
LEFT JOIN "Message" m ON u.id = m."senderId"
GROUP BY u.id, u.username
ORDER BY message_count DESC;
```

### Сброс базы данных

⚠️ **ОСТОРОЖНО! Удалит все данные!**

```bash
cd server
npx prisma migrate reset
```

---

## 💬 Тестирование WebSocket

### Использование Browser DevTools

1. Откройте DevTools (F12)
2. Перейдите в **Console**
3. Подключитесь к WebSocket:

```javascript
const socket = io('ws://localhost:3001', {
  auth: { token: 'YOUR_JWT_TOKEN' }
});

// Слушать входящие сообщения
socket.on('message', (data) => {
  console.log('Received message:', data);
});

// Отправить сообщение
socket.emit('message', {
  chatId: 'CHAT_ID',
  content: 'Hello via WebSocket!'
});

// Эмуляция набора текста
socket.emit('typing', { chatId: 'CHAT_ID', isTyping: true });
```

### Использование wscat

```bash
# Установите wscat
npm install -g wscat

# Подключитесь к WebSocket серверу
wscat -c ws://localhost:3001 \
  -H "Authorization: Bearer YOUR_TOKEN"

# Отправьте сообщение
> {"event": "message", "data": {"chatId": "...", "content": "Test"}}
```

---

## 🧰 Полезные инструменты

### Проверка здоровья сервисов

```bash
# Health check API
curl http://localhost:3001/health

# PostgreSQL
docker-compose exec postgres pg_isready

# Redis
docker-compose exec redis redis-cli ping
```

### Логи

```bash
# Все логи
docker-compose logs -f

# Только сервер
docker-compose logs -f server

# Только база данных
docker-compose logs -f postgres

# Последние 100 строк
docker-compose logs --tail=100 server
```

### Очистка Docker

```bash
# Остановить все контейнеры
docker-compose down

# Остановить и удалить volumes (БД будет очищена)
docker-compose down -v

# Пересобрать контейнеры
docker-compose up -d --build
```

---

## 🐛 Решение проблем

### Проблема: Порт уже занят

```bash
# Найти процесс на порту 5173
lsof -i :5173

# Или на порту 3001
lsof -i :3001

# Убить процесс
kill -9 <PID>
```

### Проблема: Ошибка подключения к БД

```bash
# Проверьте статус PostgreSQL
docker-compose ps postgres

# Проверьте логи
docker-compose logs postgres

# Пересоздайте контейнер
docker-compose down
docker-compose up -d postgres

# Проверьте DATABASE_URL в .env
cat server/.env | grep DATABASE_URL
```

### Проблема: Prisma ошибки

```bash
cd server

# Регенерируйте Prisma Client
npx prisma generate

# Проверьте миграции
npx prisma migrate status

# Примените миграции
npx prisma migrate dev

# Если ничего не помогает
rm -rf node_modules
npm install
npx prisma generate
```

### Проблема: CORS ошибки

Убедитесь в `server/.env`:
```env
CLIENT_URL="http://localhost:5173"
```

И в `client/.env`:
```env
VITE_API_URL=http://localhost:3001
VITE_WS_URL=ws://localhost:3001
```

### Проблема: WebSocket не подключается

1. Проверьте что сервер запущен
2. Проверьте консоль браузера на ошибки
3. Проверьте токен авторизации
4. Убедитесь что WS_URL правильный в .env

### Проблема: Файлы не загружаются

```bash
# Проверьте права на папку uploads
ls -la server/uploads

# Создайте папку если не существует
mkdir -p server/uploads
chmod 755 server/uploads

# Проверьте MAX_FILE_SIZE в .env
cat server/.env | grep MAX_FILE_SIZE
```

---

## 📊 Checklist полного тестирования

Используйте этот checklist для комплексной проверки:

### Аутентификация
- [ ] Регистрация нового пользователя
- [ ] Вход с email
- [ ] Вход с username
- [ ] Выход из системы
- [ ] Валидация форм
- [ ] JWT токен сохраняется
- [ ] Защита от SQL injection

### Профиль
- [ ] Просмотр профиля
- [ ] Редактирование профиля
- [ ] Загрузка аватара
- [ ] Изменение статуса
- [ ] Настройки приватности

### Чаты
- [ ] Создание приватного чата
- [ ] Создание группового чата
- [ ] Создание канала
- [ ] Отправка текстового сообщения
- [ ] Отправка изображения
- [ ] Отправка видео
- [ ] Отправка файла
- [ ] Запись голосового сообщения
- [ ] Ответ на сообщение
- [ ] Редактирование сообщения
- [ ] Удаление сообщения
- [ ] Упоминания (@)
- [ ] Хэштеги (#)

### Групповые функции
- [ ] Добавление участника
- [ ] Удаление участника
- [ ] Изменение роли
- [ ] Изменение настроек группы

### Звонки
- [ ] Аудио звонок
- [ ] Видео звонок
- [ ] Отключение микрофона
- [ ] Отключение видео
- [ ] История звонков

### Боты
- [ ] Создание бота
- [ ] Отправка сообщения через Bot API
- [ ] Команды бота
- [ ] Вебхуки

### PWA
- [ ] Установка как приложение
- [ ] Офлайн режим
- [ ] Push-уведомления

### Performance
- [ ] Быстрая загрузка страниц
- [ ] Плавная прокрутка списков
- [ ] Без memory leaks
- [ ] WebSocket стабилен

---

## 📚 Дополнительные ресурсы

- **[README.md](../../README.md)** - Общая информация о проекте
- **[QUICKSTART.md](../deployment/QUICKSTART.md)** - Быстрый старт
- **[DEPLOYMENT.md](../deployment/DEPLOYMENT.md)** - Развертывание в production
- **[API Documentation](../api/)** - Документация API
- **[CONTRIBUTING.md](../../CONTRIBUTING.md)** - Вклад в проект

---

## 🆘 Получить помощь

Если у вас возникли проблемы:

1. Проверьте раздел [Решение проблем](#решение-проблем)
2. Просмотрите логи: `docker-compose logs -f`
3. Создайте issue на GitHub
4. Спросите в сообществе

---

**Удачного тестирования! 🚀**
