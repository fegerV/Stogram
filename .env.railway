# Railway.app Environment Variables for Stogram
# Copy these to your Railway project settings

# Application
NODE_ENV=production
PORT=3001

# Security
JWT_SECRET=your-super-secret-jwt-key-generate-with-node-crypto
DOMAIN=your-app-name.railway.app

# Database (automatically provided by Railway)
# DATABASE_URL=postgresql://user:password@host:port/database

# Redis (if using Railway Redis service)
# REDIS_URL=redis://host:port

# Client URL (adjust based on your Railway domain)
CLIENT_URL=https://your-app-name.railway.app

# Optional: Additional configuration
# POSTGRES_USER=stogram
# POSTGRES_PASSWORD=your-secure-password  
# POSTGRES_DB=stogram_db

# Generate JWT Secret with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"