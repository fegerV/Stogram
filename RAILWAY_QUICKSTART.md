# 🚀 Быстрый деплой Stogram на Railway.app

Краткая инструкция для развертывания мессенджера Stogram на Railway.

## 📋 Что нужно сделать

### 1. Подготовка проекта

Добавьте в корень проекта файл `railway.json`:
```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd server && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

### 2. Создание проекта на Railway

1. Зайдите на [railway.app](https://railway.app/)
2. **New Project** → **Deploy from GitHub repo**
3. Выберите репозиторий с Stogram
4. Дайте проекту имя (например: `stogram-app`)

### 3. Добавление базы данных

1. В проекте нажмите **New Service**
2. Выберите **Database** → **PostgreSQL**
3. Railway автоматически добавит `DATABASE_URL`

### 4. Настройка переменных окружения

В настройках проекта добавьте:
```
NODE_ENV=production
JWT_SECRET=ваш-секретный-ключ-здесь
DOMAIN=stogram-app.railway.app
REDIS_URL=redis://host:port  # если используете Redis
```

**Генерация JWT Secret:**
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

### 5. Деплой

1. Railway автоматически начнет сборку
2. Дождитесь завершения (2-5 минут)
3. Ваше приложение будет доступно по адресу: `https://stogram-app.railway.app`

### 6. Проверка

```bash
# Проверка health endpoint
curl https://stogram-app.railway.app/health

# Открыть в браузере
open https://stogram-app.railway.app
```

## 🔧 Если что-то пошло не так

### Сборка не работает
- Проверьте логи во вкладке **Logs**
- Убедитесь, что все зависимости в `package.json`

### База данных не подключается
- Проверьте переменную `DATABASE_URL`
- Убедитесь, что PostgreSQL сервис запущен

### Frontend не загружается
- Проверьте, что сборка прошла успешно
- Проверьте routing в серверной части

## 💰 Стоимость

- **Hobby план**: $5/месяц (приложение)
- **PostgreSQL**: $5/месяц
- **Итого**: ~$10/месяц

## 📚 Полная документация

Смотрите файл `RAILWAY_DEPLOYMENT.md` для подробной инструкции.

## 🆘 Поддержка

- [Railway Docs](https://docs.railway.app/)
- [Railway Discord](https://discord.gg/railway)
- Issues в GitHub репозитории проекта